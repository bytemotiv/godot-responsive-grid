extends Control

export(String) var area

#var rng = RandomNumberGenerator.new()
		
func _ready():
	pass

	# random color and name for debug purposes, requires a ColorRect and a Label as child objects
	#rng.randomize()
	#var randomColor = Color(rng.randf_range(0,1), rng.randf_range(0,1), rng.randf_range(0,1))
	#$Name.text = area
	#$Background.color = randomColor
